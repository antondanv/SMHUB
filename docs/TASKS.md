# Текущий и следующий таск

> [!warning]
> Файл сохранен как архив старого процесса.
> Актуальные задачи и статусы теперь ведем в GitHub Issues и [GitHub Project `SMHUB Delivery`](https://github.com/users/antondanv/projects/1).

Этот файл нужен для ежедневной работы над проектом.

Здесь фиксируем:

- что делаем сейчас;
- что идёт следующим;
- что уже завершено;
- кто за что отвечает;
- что осталось проверить.

Обновлять файл нужно по мере выполнения задач.

---

# Статус проекта сейчас

## Текущий этап

**Этап 3. Справочники**

## Следующий этап

**Этап 4. Материалы (Базовый функционал)**

## Уже сделано

- [x] проект переведён в формат одного репозитория;
- [x] основной план перенесён в [PLAN.md](/home/antony/IdeaProjects/SMHUB/docs/PLAN.md);
- [x] заведена папка `docs/`;
- [x] создана базовая backend-структура;
- [x] создана базовая frontend-структура;
- [x] есть `backend/.env.example`;
- [x] есть `frontend/.env.example`;
- [x] есть `backend/requirements.txt`;
- [x] есть базовые backend-папки: `api`, `core`, `db`, `models`, `schemas`, `services`.
- [x] есть `docs/API_CONTRACT.md`;
- [x] этап 0 завершён и первая рабочая структура репозитория зафиксирована.
- [x] этап 1 завершён: база, миграции, модели, связи и seed-данные собраны.
- [x] этап 2 завершён: авторизация и профиль пользователя полностью реализованы.
- [x] добавлен Docker Compose для полного запуска `db + backend + frontend` на разных системах.

## Важно

- [ ] локальный runtime-чек backend/frontend нужно повторить после установки зависимостей в рабочем окружении.
- [ ] docker runtime-чек полного стека нужно повторить в чистом окружении после первого `docker compose up --build`.

---

# Текущий таск

## Название

**Этап 3. Справочники**

## Цель

Обеспечить работу со справочниками (курсы, направления, предметы, типы материалов) для использования в фильтрах и формах.

### Фича 3. Курсы и направления

**Ответственный:** Разработчик 1  
**Проверяющий:** Разработчик 2

- [x] `GET /courses`;
- [x] `GET /programs`;
- [ ] `CourseSelect`;
- [ ] `ProgramSelect`;
- [x] использование выбора курса и направления в профиле;
- [ ] использование выбора курса и направления в форме загрузки материала;
- [ ] использование выбора курса и направления в фильтрах каталога.

### Фича 4. Предметы и типы материалов

**Ответственный:** Разработчик 2  
**Проверяющий:** Разработчик 1

- [ ] `GET /subjects`;
- [ ] `GET /material-types`;
- [ ] `SubjectSelect`;
- [ ] `MaterialTypeSelect`;
- [ ] использование выбора предмета и типа материала в форме загрузки;
- [ ] использование выбора предмета и типа материала в фильтрах каталога;
- [ ] использование выбора предмета и типа материала на странице материала.

## Результат текущего таска

- [ ] готовы справочники курсов;
- [ ] готовы справочники направлений;
- [ ] готовы справочники предметов;
- [ ] готовы справочники типов материалов;
- [ ] справочники используются в формах и фильтрах.

---

# Следующий таск

## Название

**Этап 4. Учебные материалы**

## Начинать только после завершения текущего таска

### Фича 5. Загрузка материала

**Ответственный:** Разработчик 1  
**Проверяющий:** Разработчик 2

- [ ] `POST /materials` (загрузка файла и метаданных);
- [ ] реализация `FileService` для сохранения файлов;
- [ ] запись `file_url`, `file_name`, `file_size`, `mime_type`;
- [ ] привязка к автору, предмету, курсу, направлению и типу материала;
- [ ] статус `pending` по умолчанию;
- [ ] frontend-страница `/materials/create`;
- [ ] форма загрузки с валидацией типов и размеров файлов;
- [ ] индикация процесса загрузки.

### Фича 6. Каталог материалов

**Ответственный:** Разработчик 2  
**Проверяющий:** Разработчик 1

- [ ] `GET /materials` (фильтрация, поиск, сортировка, пагинация);
- [ ] фильтры по `subject_id`, `material_type_id`, `course_id`, `program_id`;
- [ ] сортировка по `new` и `popular`;
- [ ] frontend-страница `/materials`;
- [ ] компонент `MaterialCard`;
- [ ] панель фильтров `MaterialFilters`;
- [ ] поиск и пагинация на фронтенде.

### Фича 7. Страница одного материала

**Ответственный:** Разработчик 1  
**Проверяющий:** Разработчик 2

- [ ] `GET /materials/{id}`;
- [ ] `GET /materials/{id}/download`;
- [ ] логика инкремента `views_count` и `downloads_count`;
- [ ] frontend-страница `/materials/:id`;
- [ ] блок информации о материале и кнопки действий.

### Фича 8. Редактирование и удаление материала

**Ответственный:** Разработчик 2  
**Проверяющий:** Разработчик 1

- [ ] `PATCH /materials/{id}`;
- [ ] `DELETE /materials/{id}`;
- [ ] проверка прав автора/админа;
- [ ] frontend-страница `/materials/:id/edit`;
- [ ] функционал удаления с подтверждением.

## Результат следующего таска

- [ ] пользователь может загрузить материал;
- [ ] материал отображается в каталоге после публикации;
- [ ] работает поиск и фильтрация;
- [ ] доступно скачивание и просмотр деталей;
- [ ] автор может управлять своими материалами.

---

# Будущие этапы (кратко)

## Этап 5. Взаимодействие с материалами
- Фича 9. Комментарии (`GET`, `POST`, `PATCH`, `DELETE`).
- Фича 10. Лайки (`POST`, `DELETE`).
- Фича 11. Избранное (`POST`, `DELETE`, `GET /users/me/favorites`).
- Фича 12. Рейтинг (`POST`, `PATCH`).

## Этап 6. Главная страница и персонализация
- Фича 13. API главной страницы (подборки по курсу/направлению).
- Фича 14. Интерфейс главной страницы с персональными блоками.

## Этап 7. Модерация и полировка
- Фича 15. Модерация материалов (интерфейс для модератора).
- Фича 16. Раздел "Мои материалы" со статусами проверки.

---

# Быстрый журнал выполнения

Обновляйте коротко, без длинных описаний.

## Шаблон записи

`Дата — кто обновил — что сделал — что осталось`

## Записи

- 17.05.2026 — Обновление — Добавлен кроссплатформенный Docker-стек для разработки: `docker-compose.yml`, Dockerfile для backend/frontend, единый `run.sh`, обновлены инструкции в `README.md`. Backend теперь в compose автоматически прогоняет миграции и seed, frontend поднимается в контейнере с polling для hot reload через bind mounts. — Осталось: прогнать полный runtime smoke-test на целевой машине с Docker.
- 12.05.2026 — Обновление — Убрана аварийная инициализация БД из `FastAPI` lifespan для Vercel, добавлен ленивый bootstrap схемы и seed-данных при первом запросе. Исправлена дублирующая миграция `193590a2c807`, из-за которой production падал в середине Alembic upgrade. После повторного деплоя на `https://smhub.vercel.app` проверены `GET /api/health`, `GET /api/courses`, `GET /api/programs`, `POST /api/auth/register`, `POST /api/auth/login`. — Осталось: при желании удалить тестового пользователя из production.
- 27.04.2026 — Разработчик 2 — Инициализирован frontend (React + Vite + Router), настроен apiClient, создана главная страница с проверкой health-check. Этап 0 для Разработчика 2 завершён. — Осталось: Общая проверка этапа 0 и переход к этапу 1.
- 27.04.2026 — Разработчик 2 — Реализованы модели материалов, типов материалов, комментариев, лайков, избранного и рейтингов. Созданы перечисления ролей и статусов. Инициализирован Alembic, настроен env.py. Создан скрипт для seed-данных. Исправлен конфиг и обновлены зависимости. — Осталось: интеграция с моделями разработчика 1 и проверка общей миграции.
- 27.04.2026 — Разработчик 1 — Проверен вклад Разработчика 2, зафиксированы структура запуска, формат env и место хранения API-контракта (`docs/API_CONTRACT.md`). Этап 0 закрыт, этап 1 переведён в текущий. — Осталось: реализовать базовую архитектуру и базу данных.
- 27.04.2026 — Разработчик 1 — Добавлены модели `users`, `courses`, `programs`, `subjects`, `subject_programs`, связи, первая миграция и seed-данные для справочников. — Осталось: завершить сущности и seed-данные по зоне Разработчика 2, затем проверить этап 1 целиком.
- 27.04.2026 — Интеграция — Слиты ветки разработчиков 1 и 2, собрана единая модельная схема этапа 1 и подготовлена общая миграционная цепочка. — Осталось: стартовать этап 2.
- 29.04.2026 — Разработчик 2 — Реализован функционал профиля пользователя (Feature 2). Включает backend эндпоинты `/users/me` (GET/PATCH) и frontend страницу `/profile` с формой редактирования, выбором курса и направления. Подключены роутеры курсов и программ в `main.py`. Исправлены ошибки линтинга во всем проекте. — Осталось: реализовать авторизацию (задача Разработчика 1) для полноценной проверки профиля.
- 11.05.2026 — Обновление — Проверена реализация авторизации (Feature 1). Реализованы backend эндпоинты `/auth/register`, `/auth/login`, `/auth/me`. Настроено хеширование паролей и JWT. На frontend реализованы формы входа и регистрации, сохранение токена и автоматическая подстановка в запросы. Этап 2 полностью завершен. Частично реализован Этап 3 (справочники курсов и программ). — Осталось: реализовать справочники предметов и типов материалов, а также их использование в фильтрах и формах загрузки.

---

# Как обновлять этот файл

- если задача закончена, ставим `[x]`;
- если задача начата, но не завершена, можно дописать рядом `в работе`;
- если задача больше не актуальна, удаляем её или переносим вниз;
- после завершения текущего таска переносим следующий таск в раздел текущего;
- новый следующий таск берём из [PLAN.md](/home/antony/IdeaProjects/SMHUB/docs/PLAN.md).
